{
	"dockerimage" : "skyport/diamond:latest",
	"commands" : {
		"search" : {
			"blastx" : {
				"input" : [
						   {
						   "type"	: "file",
						   "name"	: "query"
						   },
						   {
						   "type"	: "file",
						   "name"	: "dbfile",
						   "cache" : true,
						   "description" : "filename must have suffix .dmnd !"
						   }
						   ],
				"cmd_script" :
					[
					"diamond blastx -d ${remove_extension:${dbfile}} -q ${query} -o ${remove_extension:${query}}.diamond --threads ${NumCPU}"
					],
				"outputs" : [
					{
					"name" : "result",
					"filename" : "${remove_extension:${query}}.diamond"
					}
				]
			}
		},
		"makedb" : {
			"default" : {
				"input" : [
						   {
						   "type"	: "file",
						   "name"	: "INPUT"
						   }
				],
				"cmd_script" :
					[
					"diamond makedb --in ${INPUT} --db ${remove_extension:${INPUT}} --threads ${NumCPU}"
					],
				"outputs" : [
					{
							 "name" : "diamond-db",
							 "filename" : "${remove_extension:${INPUT}}.dmnd"
					}
				]
			}
		}
	}
}